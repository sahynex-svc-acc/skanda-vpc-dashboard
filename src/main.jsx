import React, { useCallback, useEffect, useState } from 'react';
import { createRoot } from 'react-dom/client';
import { signInWithPopup, signOut } from 'firebase/auth';
import { firebaseAuth, googleProvider } from './firebase';
import './style.css';

const api = async (path, options = {}) => {
  const res = await fetch(`/api${path}`, { credentials:'include', ...options, headers:{'Content-Type':'application/json',...(options.headers||{})} });
  const data = await res.json().catch(()=>({}));
  if(res.status===401) throw new Error('AUTH_REQUIRED');
  if(!res.ok) throw new Error(data.detail||'Request failed');
  return data;
};
const transitional=new Set(['deploying','starting','stopping','restarting']);
const pretty=s=>(s||'unknown').replace(/_/g,' ');

function Icon({name,size=17}){const p={width:size,height:size,viewBox:'0 0 24 24',fill:'none',stroke:'currentColor',strokeWidth:1.8,strokeLinecap:'round',strokeLinejoin:'round'};const d={grid:<><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></>,box:<><path d="m12 3 8 4.5v9L12 21l-8-4.5v-9L12 3Z"/><path d="m4 7.5 8 4.5 8-4.5M12 12v9"/></>,git:<path d="M9 19c-4 1-4-2-5-2m10 4v-3.9c0-1.1.1-1.5-.5-2.1 2-.2 4.1-1 4.1-4.6 0-1-.4-1.8-1-2.4.1-.3.4-1.2-.1-2.4 0 0-.8-.3-2.6 1a9 9 0 0 0-4.8 0c-1.8-1.2-2.6-1-2.6-1-.5 1.2-.2 2.1-.1 2.4-.6.6-1 1.4-1 2.4 0 3.6 2.1 4.4 4.1 4.6-.6.5-.6 1.1-.5 2.1V21"/>,activity:<path d="M3 12h4l2-7 4 14 2-7h6"/>,plus:<><path d="M12 5v14M5 12h14"/></>,refresh:<><path d="M20 11a8 8 0 0 0-14.8-4L3 10"/><path d="M3 5v5h5"/><path d="M4 13a8 8 0 0 0 14.8 4L21 14"/><path d="M21 19v-5h-5"/></>,play:<path d="m8 5 11 7-11 7V5Z"/>,stop:<rect x="6" y="6" width="12" height="12" rx="1"/>,external:<><path d="M14 4h6v6"/><path d="M10 14 20 4"/><path d="M20 13v6a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V5a1 1 0 0 1 1-1h6"/></>,shield:<><path d="M12 3 20 6v5c0 5-3.5 8.5-8 10-4.5-1.5-8-5-8-10V6l8-3Z"/><path d="m9 12 2 2 4-4"/></>,menu:<><path d="M4 6h16M4 12h16M4 18h16"/></>,close:<><path d="M6 6l12 12M18 6 6 18"/></>,check:<path d="m5 12 4 4L19 6"/>};return <svg {...p}>{d[name]||d.box}</svg>}

function App(){const[me,setMe]=useState(null),[apps,setApps]=useState([]),[health,setHealth]=useState(null),[view,setView]=useState('overview'),[selected,setSelected]=useState(null),[deploy,setDeploy]=useState(false),[loading,setLoading]=useState(true),[refreshing,setRefreshing]=useState(false),[actions,setActions]=useState({}),[toast,setToast]=useState('');
 const load=useCallback(async(silent=true)=>{try{const[m,a,h]=await Promise.all([api('/me'),api('/apps'),api('/health')]);setMe(m.user);setApps(Array.isArray(a)?a:(a.apps||[]));setHealth(h)}catch(e){if(e.message==='AUTH_REQUIRED')setMe(null);else if(!silent)setToast(e.message)}finally{setLoading(false);setRefreshing(false)}},[]);
 useEffect(()=>{load();const t=setInterval(()=>load(),2500);return()=>clearInterval(t)},[load]);
 useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),3500);return()=>clearTimeout(t)},[toast]);
 const action=async(id,verb)=>{setActions(x=>({...x,[id]:verb}));try{await api(`/apps/${id}/${verb}`,{method:'POST'});setToast(`${verb[0].toUpperCase()+verb.slice(1)} requested`);await load(false)}catch(e){setActions(x=>{const n={...x};delete n[id];return n});setToast(e.message)}};
 if(loading)return <div className="splash"><div className="brandMark">S</div><span>Loading Skanda VPC</span></div>;
 if(!me)return <Login/>;
 const stats={total:apps.length,running:apps.filter(a=>a.status==='running').length,stopped:apps.filter(a=>a.status==='stopped').length,updating:apps.filter(a=>transitional.has(a.status)||actions[a.app_id]).length};
 const online=health?.status==='healthy'||health?.status==='ok';
 const open=a=>{setSelected(a);setView('application')};
 return <div className="appShell"><aside className="sidebar"><div className="brand"><div className="brandMark">S</div><div><strong>Skanda VPC</strong><small>Private cloud platform</small></div></div><div className="navLabel">WORKSPACE</div><Nav active={view==='overview'} icon="grid" text="Overview" click={()=>setView('overview')}/><Nav active={view==='deployments'||view==='application'} icon="box" text="Deployments" click={()=>setView('deployments')}/><Nav active={view==='repositories'} icon="git" text="Repositories" click={()=>setView('repositories')}/><Nav active={view==='activity'} icon="activity" text="Activity" click={()=>setView('activity')}/><div className="sidebarBottom"><div className="statusLine"><i className={`dot ${online?'online':'offline'}`}/>{online?'Control plane online':'Control plane offline'}</div><div className="version">Skanda VPC · 1.1</div></div></aside><main className="content"><header className="topbar"><div className="crumb">Workspace <span>/</span> {view==='application'?'Application':view[0].toUpperCase()+view.slice(1)}</div><div className="account">{me.picture&&<img src={me.picture} className="avatarImg"/>}<div className="accountText"><b>{me.name}</b><small>{me.email}</small></div><button className="secondary signOut" onClick={async()=>{await api('/auth/logout',{method:'POST'});await signOut(firebaseAuth);location.reload()}}>Sign out</button></div></header>{view==='overview'&&<Overview stats={stats} apps={apps} online={online} deploy={()=>setDeploy(true)} open={open} action={action} actions={actions} refresh={()=>{setRefreshing(true);load(false)}} refreshing={refreshing}/>} {view==='deployments'&&<Deployments apps={apps} deploy={()=>setDeploy(true)} open={open} action={action} actions={actions}/>} {view==='repositories'&&<Repositories deploy={()=>setDeploy(true)}/>} {view==='activity'&&<Activity apps={apps}/>} {view==='application'&&selected&&<Application app={apps.find(a=>a.app_id===selected.app_id)||selected} back={()=>setView('deployments')} action={action} actions={actions}/>}</main>{deploy&&<DeployModal close={()=>setDeploy(false)} done={()=>{setDeploy(false);load(false);setView('deployments')}}/>}{toast&&<div className="toast">{toast}</div>}</div>}
function Login(){const[busy,setBusy]=useState(false),[error,setError]=useState('');const login=async()=>{setBusy(true);setError('');try{const r=await signInWithPopup(firebaseAuth,googleProvider);await api('/auth/firebase',{method:'POST',body:JSON.stringify({idToken:await r.user.getIdToken(true)})});location.reload()}catch(e){setError('Sign-in failed. Please try again.')}finally{setBusy(false)}};return <div className="login"><div className="loginCard"><div className="brandMark big">S</div><div className="eyebrow">SKANDA VPC</div><h1>Deploy with confidence.</h1><p>Private application deployments with GitHub, isolated runtimes, and a simple control plane.</p><button className="googleBtn" onClick={login} disabled={busy}><b>G</b>{busy?'Signing in…':'Continue with Google'}</button>{error&&<div className="error">{error}</div>}<div className="loginSecurity"><Icon name="shield" size={15}/> Only authorized accounts can deploy</div></div></div>}
function Nav({active,icon,text,click}){return <button className={`navItem ${active?'active':''}`} onClick={click}><Icon name={icon}/>{text}</button>}
function Header({title,sub,action}){return <div className="pageHeader"><div><div className="eyebrow">WORKSPACE</div><h1>{title}</h1><p>{sub}</p></div>{action}</div>}
function Overview({stats,apps,online,deploy,open,action,actions,refresh,refreshing}){return <><Header title="Overview" sub="Monitor your applications and deploy new workloads." action={<button className="primary" onClick={deploy}><Icon name="plus"/> New deployment</button>}/><div className="stats"><Stat l="Applications" v={stats.total}/><Stat l="Running" v={stats.running} tone="green"/><Stat l="Stopped" v={stats.stopped}/><Stat l="Control plane" v={online?'Online':'Offline'} tone={online?'green':'red'} sub={online?'Healthy':'Unavailable'}/></div>{stats.updating>0&&<div className="liveNotice"><span className="statusDot yellow"/><div><b>{stats.updating} deployment{stats.updating>1?'s':''} updating</b><span>Status refreshes automatically.</span></div></div>}<section className="section"><div className="sectionHead"><div><h2>Applications</h2><p>Live deployment status across your workspace.</p></div><button className="refreshBtn" onClick={refresh} disabled={refreshing}><Icon name="refresh" size={14}/>{refreshing?'Refreshing':'Refresh'}</button></div><Table apps={apps.slice(0,8)} open={open} action={action} actions={actions}/></section></>}
function Stat({l,v,tone,sub}){return <div className="statCard"><span>{l}</span><strong>{v}</strong>{sub&&<i className={`statSub ${tone}`}><span className="statusDot"/>{sub}</i>}</div>}
function Deployments({apps,deploy,open,action,actions}){return <><Header title="Deployments" sub="Build, run and manage your applications." action={<button className="primary" onClick={deploy}><Icon name="plus"/> New deployment</button>}/><section className="section"><div className="sectionHead"><div><h2>All deployments</h2><p>Live status updates are automatic.</p></div><span className="livePill"><span className="statusDot green"/> Live</span></div><Table apps={apps} open={open} action={action} actions={actions}/></section></>}
function Table({apps,open,action,actions={}}){if(!apps.length)return <div className="empty"><div className="emptyIcon"><Icon name="box" size={22}/></div><h3>No deployments yet</h3><p>Deploy your first application to see it here.</p></div>;return <div className="table"><div className="tr th"><span>APPLICATION</span><span>STATUS</span><span>RUNTIME</span><span>PORT</span><span>ACTIONS</span></div>{apps.map(a=>{const pending=actions[a.app_id];const status=pending?(pending==='restart'?'restarting':pending):a.status;return <div className="tr" key={a.app_id}><button className="appName" onClick={()=>open(a)}><span className="appIcon"><Icon name="box" size={15}/></span><span><b>{a.app_id}</b><small>{a.repo?.split('/').pop()?.replace('.git','')||a.framework||'application'}</small></span></button><Status status={status}/><span className="mutedCell">{a.runtime||'—'}{a.framework?` · ${a.framework}`:''}</span><span className="mutedCell">{a.port||'—'}</span><span className="rowActions"><button disabled={!!pending} title={a.status==='running'?'Stop':'Start'} onClick={()=>action(a.app_id,a.status==='running'?'stop':'start')}>{a.status==='running'?<Icon name="stop" size={14}/>:<Icon name="play" size={14}/>}</button><button disabled={!!pending} title="Restart" onClick={()=>action(a.app_id,'restart')}><Icon name="refresh" size={14}/></button>{a.public_id&&<a className="openBtn" href={`/a/${a.public_id}`} target="_blank" rel="noreferrer" title="Open application"><Icon name="external" size={14}/></a>}</span></div>})}</div>}
function Status({status}){const s=status||'unknown';const c=transitional.has(s)?`status-${s}`:s==='running'?'status-running':s==='stopped'?'status-stopped':s==='failed'?'status-failed':'status-unknown';return <span className={`statusBadge ${c}`}><span className="dot"/>{pretty(s)}</span>}
function Repositories({deploy}){return <><Header title="Repositories" sub="Deploy applications from connected GitHub repositories." action={<button className="primary" onClick={deploy}><Icon name="plus"/> Deploy repository</button>}/><div className="repoCard"><div className="repoLogo"><Icon name="git" size={23}/></div><div className="repoMain"><h2>GitHub</h2><p>Repository source is handled by the Skanda VPC control plane.</p><span className="connected"><span className="statusDot green"/> Connected</span></div></div></>}
function Activity({apps}){return <><Header title="Activity" sub="Recent application state across your workspace."/><div className="activityList">{apps.map(a=><div className="activityItem" key={a.app_id}><div className="activityIcon"><Icon name={a.status==='running'?'play':'stop'} size={14}/></div><div><b>{a.app_id}</b><p>Application is {pretty(a.status)} on port {a.port||'—'}.</p></div><span>{a.runtime||'runtime'}</span></div>)}</div></>}
function Application({app,back,action,actions}){const[logs,setLogs]=useState('');const getLogs=async()=>{try{const d=await api(`/apps/${app.app_id}/logs?lines=200`);setLogs(d.lines||'')}catch(e){setLogs(e.message)}};useEffect(()=>{getLogs()},[app.app_id]);const pending=actions[app.app_id],status=pending?(pending==='restart'?'restarting':pending):app.status;return <><button className="back" onClick={back}>← Back to deployments</button><div className="appHero"><div className="appTitle"><span className="appIcon large"><Icon name="box" size={20}/></span><div><div className="eyebrow">APPLICATION</div><h1>{app.app_id}</h1><div className="heroMeta"><Status status={status}/><span>{app.runtime||'—'}</span><span>{app.framework||'—'}</span><span>Port {app.port||'—'}</span></div></div></div><div className="heroActions"><button className="secondary" disabled={!!pending} onClick={()=>action(app.app_id,app.status==='running'?'stop':'start')}>{app.status==='running'?'Stop':'Start'}</button><button className="primary" disabled={!!pending} onClick={()=>action(app.app_id,'restart')}>{pending?'Restarting…':'Restart'}</button>{app.public_id&&<a className="secondary" href={`/a/${app.public_id}`} target="_blank" rel="noreferrer">Open app <Icon name="external" size={13}/></a>}</div></div><div className="logCard"><div className="sectionHead"><div><h2>Deployment logs</h2><p>Latest output from the application.</p></div><button className="secondary" onClick={getLogs}>Refresh</button></div><pre>{logs||'No logs available.'}</pre></div></>}
function DeployModal({close,done}){const[repo,setRepo]=useState(''),[branch,setBranch]=useState('main'),[repoId,setRepoId]=useState(''),[dirs,setDirs]=useState([]),[dir,setDir]=useState(''),[vars,setVars]=useState(''),[busy,setBusy]=useState(false),[inspecting,setInspecting]=useState(false),[error,setError]=useState('');const inspect=async()=>{setInspecting(true);setError('');try{const d=await api('/repositories/inspect',{method:'POST',body:JSON.stringify({repo:repo.trim(),branch:branch.trim()||'main'})});setRepoId(d.repo_id);const found=Array.isArray(d.directories)?d.directories:(Array.isArray(d.tree)?d.tree.filter(x=>x.type==='directory').map(x=>x.path):[]);setDirs(found);setDir(found.includes('')?'':found[0]||'')}catch(e){setError(e.message)}finally{setInspecting(false)}};const submit=async e=>{e.preventDefault();if(!repoId){setError('Inspect the repository first.');return}setBusy(true);setError('');const env={};vars.split('\n').forEach(x=>{const i=x.indexOf('=');if(i>0)env[x.slice(0,i).trim()]=x.slice(i+1)});try{await api('/deploy',{method:'POST',body:JSON.stringify({repo_id:repoId,project_dir:dir,env})});done()}catch(e){setError(e.message)}finally{setBusy(false)}};return <div className="modalBackdrop"><form className="modal deployModal" onSubmit={submit}><div className="modalHead"><div><div className="eyebrow">NEW DEPLOYMENT</div><h2>Deploy application</h2><p>Connect a GitHub repository, choose a project, and deploy.</p></div><button className="close" type="button" onClick={close}><Icon name="close"/></button></div><div className="field"><label>GitHub repository</label><div className="inputAction"><input required value={repo} onChange={e=>{setRepo(e.target.value);setRepoId('');setDirs([])}} placeholder="https://github.com/owner/project.git"/><button type="button" className="secondary" onClick={inspect} disabled={inspecting||!repo.trim()}>{inspecting?'Inspecting…':'Inspect'}</button></div></div><div className="field"><label>Branch</label><input value={branch} onChange={e=>setBranch(e.target.value)} placeholder="main"/></div>{repoId&&<div className="inspected"><span className="check"><Icon name="check" size={13}/></span><div><b>Repository inspected</b><small>Ready to select a project directory.</small></div></div>}<div className="field"><label>Project directory <span>optional</span></label>{dirs.length?<select value={dir} onChange={e=>setDir(e.target.value)}><option value="">Repository root</option>{dirs.map(x=><option key={x} value={x}>{x}</option>)}</select>:<input value={dir} onChange={e=>setDir(e.target.value)} placeholder={repoId?'Repository root':'Inspect repository first'}/>}</div><div className="field"><label>Environment variables <span>optional</span></label><textarea value={vars} onChange={e=>setVars(e.target.value)} placeholder={'NODE_ENV=production\nAPI_URL=https://example.com'}/></div>{error&&<div className="error">{error}</div>}<div className="modalFoot"><button type="button" className="secondary" onClick={close}>Cancel</button><button className="primary" disabled={busy||!repoId}>{busy?'Deploying…':'Deploy application'}</button></div></form></div>}
createRoot(document.getElementById('root')).render(<App/>);
