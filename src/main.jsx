import React,{useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
const API='/api';
function App(){const[user,setUser]=useState(null);const[repo,setRepo]=useState('');const[dir,setDir]=useState('');const[message,setMessage]=useState('');const[status,setStatus]=useState('');
function signIn(){window.location.href=API+'/auth/google'}
async function deploy(){if(!repo)return setStatus('Select a repository first.');setStatus('Deploying…');try{const r=await fetch(API+'/deploy',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({repo_id:repo,project_dir:dir,env:message?{DEPLOY_MESSAGE:message}:{}})});const d=await r.json();setStatus(r.ok?`Deployment ${d.status}. App: ${d.app?.public_id||'created'}`:(d.detail||'Deployment failed'));}catch(e){setStatus('Control plane unavailable.')}}
return <div className="shell"><aside><div className="brand"><span className="mark">S</span><div><b>Skanda VPC</b><small>Control Plane</small></div></div><nav><a className="active">Deployments</a><a>Applications</a><a>Repositories</a><a>Activity</a></nav><div className="sidefoot">Private deployment platform<br/>v1.0</div></aside><main><header><div><p className="eyebrow">CONTROL PLANE</p><h1>Deployments</h1><p className="muted">Deploy applications from your connected GitHub repositories.</p></div>{user?<div className="user"><span>{user.name||user.email}</span><button onClick={()=>setUser(null)}>Sign out</button></div>:<button className="google" onClick={signIn}>Continue with Google</button>}</header>{!user?<section className="gate"><div className="lock">⌁</div><h2>Authentication required</h2><p>Sign in with your authorized Google account to access deployments.</p><button className="primary" onClick={signIn}>Sign in with Google</button></section>:<section className="grid"><div className="card deploy"><div className="cardhead"><div><h2>New deployment</h2><p>Choose a repository and project directory.</p></div><span className="pill">Authenticated</span></div><label>GitHub repository</label><input value={repo} onChange={e=>setRepo(e.target.value)} placeholder="Repository ID or owner/name"/><label>Project directory <em>optional</em></label><input value={dir} onChange={e=>setDir(e.target.value)} placeholder="e.g. frontend"/><label>Deployment variable <em>optional</em></label><input value={message} onChange={e=>setMessage(e.target.value)} placeholder="DEPLOY_MESSAGE value"/><button className="primary wide" onClick={deploy}>Deploy application <span>→</span></button>{status&&<div className="status">{status}</div>}</div><div className="card info"><h2>Platform</h2><div className="stat"><b>GitHub</b><span>Connected</span></div><div className="stat"><b>Runtime</b><span>Auto-detected</span></div><div className="stat"><b>Routing</b><span>Public proxy</span></div><div className="note">Deployments are built and served by the Skanda VPC control plane. Only authenticated users can initiate deployments.</div></div></section>}</main></div>};
createRoot(document.getElementById('root')).render(<App/>);
